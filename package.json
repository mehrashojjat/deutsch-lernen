{
  "name": "wortschatz",
  "description": "German vocabulary app — static web root with npm dev server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
